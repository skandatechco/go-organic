# Suppress peer dependency warnings from transitive dependencies
legacy-peer-deps=false

# Use npm's new resolver for better dependency resolution
node-linker=hoisted

# Ignore deprecation warnings from dependencies we can't control
audit=false

